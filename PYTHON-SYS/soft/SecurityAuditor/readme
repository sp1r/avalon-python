Приложение, производящее аудит безопасности файловой системы.

Функциональные возможности:

Вывод информации о файлах с установленными suid и sgid битами (имя файла, имя владельца, имя группы, дата последнего изменения).
Вывод информации о файлах, находящихся в каталогах /bin, /sbin, /usr/bin, /usr/sbin, /lib, /usr/lib и /etc, доступных для записи не только пользователю root и группе wheel.
Генерация отчета в удобной для чтения форме.
